# Ignore everything in scratch by default (ns-3 default)
/*
!.gitignore

!nested-subdir/
!subdir/
!scratch-simulator.cc
!CMakeLists.txt

# ── Project source files (un-ignored explicitly) ──────────────────────────────
!wsn-protocol-model.h
!wsn-diag.cc
!lecmac-sim.cc
!leach-sim.cc
!esmac-sim.cc
!lecmac-plus-sim.cc
